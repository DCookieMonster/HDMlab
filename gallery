


<style type="text/css">
    .arrow {
        cursor: pointer;
        display: block;
        height: 64px;
        margin-top: -35px;
        outline: medium none;
        position: absolute;
        top: 50%;
        width: 64px;
        z-index: 5;
    }
    .arrow.prev {
        background-image: url("img/prev.jpg");
        background-size: 100% 100%;
        background-repeat: no-repeat;
        left: 20px;
        opacity: 0.2;
        transition: all 0.2s linear 0s;
    }
    .arrow.next {
        background-image: url("img/next.png");
        background-size: 100% 100%;
        background-repeat: no-repeat;
        opacity: 0.2;
        right: 20px;
        transition: all 0.2s linear 0s;
    }
    .arrow.prev:hover{
        opacity:1;
    }
    .arrow.next:hover{
        opacity:1;
    }
    .navig {
        bottom: -3px;
        display: block;
        height: 48px;
        left: 0;
        margin: 0 auto;
        padding: 1em 0 0.8em;
        position: absolute;
        right: 0;
        text-align: center;
        width: 100%;
        z-index: 5;
    }
    .navig li {
        border: 3px solid #AAAAAA;
        border-radius: 3px;
        cursor: pointer;
        display: inline-block;
        height: 30px;
        margin: 0 8px;
        position: relative;
        width: 50px;
    }
    .navig li.active {
        border: 5px solid #FFFFFF;
    }
    .navig li img {
        width: 100%;
    }
    .slider {
        border: 15px solid #FFFFFF;
        border-radius: 5px;
        height: 600px;
        margin: 20px auto;
        position: relative;
        width: 900px;
        -webkit-perspective: 1000px;
        -moz-perspective: 1000px;
        -ms-perspective: 1000px;
        -o-perspective: 1000px;
        perspective: 1000px;
        -webkit-transform-style: preserve-3d;
        -moz-transform-style: preserve-3d;
        -ms-transform-style: preserve-3d;
        -o-transform-style: preserve-3d;
        transform-style: preserve-3d;
    }
    .slide {
        position: absolute;
        top: 0;
        left: 0;
    }

    .slide.ng-hide-add.ng-hide-add-active {
        opacity:0;
    }
    .slide.ng-hide-remove {
        -webkit-transition:1s linear all;
        -moz-transition:1s linear all;
        -o-transition:1s linear all;
        transition:1s linear all;
        display:block!important;
        opacity:0;
    }
    .slide, .slide.ng-hide-remove.ng-hide-remove-active {
        opacity:1;
    }

</style>


<div ng-controller="galleryCtrl" class="container content-section ">
    <h1>Gallery</h1>
    <hr>
    <br><br>

    <div class="container slider">
        <!-- enumerate all photos -->

        <img ng-repeat="photo in images" class="slide" ng-swipe-right="showPrev()" ng-swipe-left="showNext()" ng-show="isActive($index)" ng-src="{{photo.src}}" />

        <!-- prev / next controls -->

        <a class="arrow prev" href="" ng-click="showPrev()"></a>

        <a class="arrow next" href="" ng-click="showNext()"></a>

        <!-- extra navigation controls -->

        <ul class="navig">

            <li ng-repeat="photo in images" ng-class="{'active':isActive($index)}">

                <img src="{{photo.src}}" alt="{{photo.desc}}" title="{{photo.desc}}" ng-click="showPhoto($index);" />

            </li>

        </ul>

    </div>



</div>

</div>

